import React from 'react';
import { EducationItem } from '../types';

export const Education: React.FC = () => {
  return null; // Integrated into Experience or About for simplicity in this specific file structure
};